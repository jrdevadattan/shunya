export function ReadErrorMap({ rescued, unreadable, pending }: { rescued: number; unreadable: number; pending: number }) {
  return <figure className="read-error-map"><figcaption><strong>Device read map</strong><span>Live acquisition coverage</span></figcaption><div role="img" aria-label={`${rescued}% rescued, ${unreadable}% unreadable, ${pending}% pending`} className="read-error-map__bar"><span className="is-rescued" style={{ width: `${rescued}%` }} /><span className="is-unreadable" style={{ width: `${unreadable}%` }} /><span className="is-pending" style={{ width: `${pending}%` }} /></div><ul><li><span className="is-rescued" />{rescued}% rescued</li><li><span className="is-unreadable" />{unreadable}% unreadable</li><li><span className="is-pending" />{pending}% pending</li></ul></figure>;
}
