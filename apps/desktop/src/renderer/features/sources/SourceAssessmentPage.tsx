import { RecoverySetupPage } from '../recovery/RecoverySetupPage.js';

/** Legacy route: the source check is shown on the recovery set-up screen. */
export function SourceAssessmentPage() {
  return <RecoverySetupPage />;
}
