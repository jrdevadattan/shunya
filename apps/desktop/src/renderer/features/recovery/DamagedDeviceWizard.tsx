export function DamagedDeviceWizard() {
  return <section className="workflow-page"><header><p className="eyebrow">Rescue Mode only</p><h1>Damaged device recovery</h1><p>Repeated reading can worsen a failing device. A verified ddrescue workflow must image healthy regions first and persist its mapfile.</p></header><aside className="report-limitations"><h2>Damaged-device workflow unavailable</h2><code>DDRESCUE_UI_UNAVAILABLE</code><p>The current daemon does not expose typed ddrescue progress or controls. No rescued, unreadable, or pending ranges are simulated.</p></aside><button className="button button--primary" type="button" disabled>Start first pass</button></section>;
}
