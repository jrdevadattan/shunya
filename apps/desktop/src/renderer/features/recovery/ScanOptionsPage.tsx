import { RecoverySetupPage } from './RecoverySetupPage.js';

/** Legacy route: scan options now live on the single recovery set-up screen. */
export function ScanOptionsPage() {
  return <RecoverySetupPage />;
}
